/* A tabela virtualizada virou componente compartilhado (reusada em
   Contatos, F7). Este arquivo mantém o nome local que a Lista importa. */
export { TabelaVirtual as TabelaNegocios } from "@/components/dominio/tabela-virtual";
